# Public-by-design browser credential. Never put an sk.* token here.
# Replace only with a dedicated, minimum-scope, URL-restricted atlas token.
VITE_MAPBOX_PUBLIC_TOKEN=pk.replace_with_dedicated_restricted_atlas_token
